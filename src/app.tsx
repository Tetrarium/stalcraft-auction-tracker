import "./global.sass";
import "bootstrap/scss/bootstrap.scss";

import { FC } from "react";

import Layout from "./components/layout";

const App: FC = () => {
  return (
    <Layout>
      Main
    </Layout>
  );
};

export default App;